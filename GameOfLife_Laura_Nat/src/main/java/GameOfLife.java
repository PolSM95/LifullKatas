import java.util.ArrayList;

public class GameOfLife {
    public ArrayList<Seed> life(ArrayList<Seed> actual) {
        ArrayList<Seed> next = new ArrayList<>();
        if (actual.size()<=2) {
            return next;
        }
            for (Seed seed:actual) {
            }


        return next;
    }
}
